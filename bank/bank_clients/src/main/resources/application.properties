spring.datasource.url=jdbc:mysql://${BANK_CLIENTS_DATABASE_URL:"localhost:3306"}/${BANK_CLIENTS_DATABASE_NAME:bank_clients}
spring.datasource.username=${BANK_CLIENTS_DATABASE_USERNAME:root}
spring.datasource.password=${BANK_CLIENTS_DATABASE_PASSWORD:42424242}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

spring.mvc.pathmatch.matching-strategy=ant_path_matcher
spring.jpa.properties.hibernate.default_storage_engine=InnoDB

# Change to port 5000 (because of Nginx on AWS)
server.port=5000


